kind: Deployment
apiVersion: apps/v1
metadata:
  name: calproxy
  labels:
    app: calproxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calproxy
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: calproxy
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
    spec:
      containers:
        - name: calproxy
          image: gcr.io/com-seankhliao/calproxy:latest
          env:
            - name: PORT
              value: "80"
          envFrom:
            - secretRef:
                name: calproxy-config
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /ping
              port: 80
          livenessProbe:
            httpGet:
              path: /ping
              port: 80
