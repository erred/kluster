namespace: auth
commonLabels:
  app: pomerium
resources:
  - deployment.yaml
  - namespace.yaml
  - service.yaml
  - secret.yaml
configMapGenerator:
  - name: pomerium
    files:
      - config.yaml
images:
  - name: pomerium/pomerium
    newTag: v0.7.0
