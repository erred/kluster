apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - rbac.yaml
  - service.yaml
  - secret.yaml
configMapGenerator:
  - files:
      - config/config.yaml
    name: pomerium
images:
  - name: pomerium/pomerium
    newTag: v0.10.3
