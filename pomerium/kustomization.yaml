apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.k8s.yaml
  - rbac.k8s.yaml
  - service.k8s.yaml
  - secret.k8s.yaml
configMapGenerator:
  - files:
      - config/config.yaml
    name: pomerium
images:
  - name: pomerium/pomerium
    newTag: v0.10.3
