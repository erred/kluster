namespace: monitor
resources:
  - deployment.yaml
  - rbac.yaml
  - security.yaml
  - service.yaml
  - volume.yaml
configMapGenerator:
  - name: grafana
    files:
      - grafana.ini
  - name: grafana-provisioning-datasources
    files:
      - loki.yaml
      - prometheus.yaml
