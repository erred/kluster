kind: Deployment
apiVersion: apps/v1
metadata:
  name: http-server
spec:
  template:
    spec:
      containers:
        - name: http-server
          resources:
            requests:
              cpu: 20m
              memory: 40Mi
            limits:
              cpu: 40m
              memory: 80Mi
