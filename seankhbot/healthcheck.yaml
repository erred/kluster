kind: Deployment
apiVersion: apps/v1
metadata:
  name: seankhbot
spec:
  template:
    spec:
      containers:
        - name: seankhbot
          livenessProbe:
            initialDelaySeconds: 2
            periodSeconds: 2
            httpGet:
              path: /health
              port: 8080
