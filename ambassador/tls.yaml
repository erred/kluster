apiVersion: getambassador.io/v1
kind: TLSContext
metadata:
  name: cf-tls
spec:
  hosts: ["*"]
  secret: cf-certs-tls-secret
  redirect_cleartext_from: 8080
# ---
# apiVersion: getambassador.io/v1
# kind: TLSContext
# metadata:
#   name: le-tls
# spec:
#   hosts: ["*.seankhliao.com"]
#   secret: le-cert.cert-manager
#   redirect_cleartext_from: 8080
