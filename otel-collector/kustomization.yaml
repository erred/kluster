kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
resources:
  - deployment.yaml
  - rbac.yaml
  - service.yaml
configMapGenerator:
  - name: otel-collector
    files:
      - config/config.yaml
images:
  - name: otel/opentelemetry-collector
    # https://github.com/open-telemetry/opentelemetry-collector/issues/1867
    newTag: 0.10.0
