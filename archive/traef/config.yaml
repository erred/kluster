apiVersion: v1
kind: ConfigMap
metadata:
  name: traefikconf
  namespace: kube-system
data:
  traefik.toml: |
    defaultEntryPoints = ["http"]

    [entryPoints]

    [entryPoints.http]
      address = ":80"
