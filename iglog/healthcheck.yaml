kind: Deployment
apiVersion: apps/v1
metadata:
  name: iglog
spec:
  template:
    spec:
      containers:
        - name: iglog
          livenessProbe:
            initialDelaySeconds: 2
            periodSeconds: 2
            httpGet:
              path: /health
              port: 8080
          readinessProbe:
            initialDelaySeconds: 2
            periodSeconds: 2
            httpGet:
              path: /ready
              port: 8080
