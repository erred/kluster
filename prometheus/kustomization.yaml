namespace: monitor
commonLabels:
  app: prometheus
resources:
  - deployment.yaml
  - rbac.yaml
  - service.yaml
  - volume.yaml
configMapGenerator:
  - name: prometheus
    files:
      - prometheus.yml
images:
  - name: prom/prometheus
    newTag: v2.17.1
