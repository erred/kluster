apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - rbac.yaml
  - service.yaml
  - volume.yaml
configMapGenerator:
  - files:
      - config/prometheus.yml
    name: prometheus
images:
  - name: prom/prometheus
    newTag: v2.21.0
