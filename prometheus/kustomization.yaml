namespace: monitor
resources:
  - daemonset.yaml
  - deployment.yaml
  - rbac.yaml
  - service.yaml
  - statefulset.yaml
  - volume.yaml
configMapGenerator:
  - name: prometheus-server
    files:
      - prometheus.yml
