apiVersion: v1
kind: ConfigMap
metadata:
  name: traefikconf
  namespace: kube-system
data:
  traefik.toml: |
    [api]
      dashboard = true

    [entrypoints.web]
      address = ":80"
    [entrypoints.webs]
      address = ":443"

    [providers.kubernetes]
