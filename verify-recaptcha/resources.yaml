kind: Deployment
apiVersion: apps/v1
metadata:
  name: verify-recaptcha
spec:
  template:
    spec:
      containers:
        - name: verify-recaptcha
          resources:
            requests:
              cpu: 20m
              memory: 40Mi
            limits:
              cpu: 40m
              memory: 80Mi
