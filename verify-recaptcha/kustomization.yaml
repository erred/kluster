patchesStrategicMerge:
  - preemptible.yaml
  - resources.yaml
  - config.yaml
patchesJson6902:
  - path: ingress.yaml
    target:
      group: traefik.containo.us
      kind: IngressRoute
      name: verify-recaptcha
      version: v1alpha1
resources:
  - base
