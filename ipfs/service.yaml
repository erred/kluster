apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: jaeger
spec:
  entryPoints:
    - https
  routes:
    - kind: Rule
      match: Host(`ipfs.seankhliao.com`)
      services:
        - kind: Service
          name: ipfs
          port: 4001
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: ipfs
  name: ipfs
spec:
  ports:
    - name: swarm
      port: 4001
      targetPort: swarm
    - name: api
      port: 5001
      targetPort: api
    - name: gateway
      port: 8080
      targetPort: gateway
  selector:
    app: ipfs
  type: "ClusterIP"
