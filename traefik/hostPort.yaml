kind: Deployment
apiVersion: apps/v1
metadata:
  name: traefik
spec:
  template:
    spec:
      containers:
        - name: traefik
          ports:
            - name: web
              containerPort: 8080
              hostPort: 80
            - name: websecure
              containerPort: 8443
              hostPort: 443
