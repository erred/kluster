namespace: traefik
commonLabels:
  app: traefik
resources:
  - cert.yaml
  - crd.yaml
  - deployment.yaml
  - rbac.yaml
configMapGenerator:
  - name: traefik
    files:
      - traefik.yaml
images:
  - name: traefik
    newTag: v2.2.0
