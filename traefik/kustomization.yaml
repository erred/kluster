namespace: networking
commonLabels:
  app: traefik
resources:
  - cert.yaml
  - crd.yaml
  - deployment.yaml
  - rbac.yaml
  - service.yaml
configMapGenerator:
  - name: traefik-config
    files:
      - traefik.yaml
