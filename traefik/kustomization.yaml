namespace: networking
commonLabels:
  app: traefik
resources:
  - crd.yaml
  - deployment.yaml
  - rbac.yaml
  - service.yaml
configMapGenerator:
  - name: traefik-config
    files:
      - traefik.yaml
