apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: readss
spec:
  entryPoints:
    - websecure
  tls:
    secretName: tls-prod
  routes:
    - kind: Rule
      match: Host(`*`)
      priority: 10
      services:
        - name: readss
          port: 80
