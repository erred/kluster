apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jaeger
spec:
  resources:
    requests:
      storage: 50Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeMode: Filesystem
  volumeName: jaeger
