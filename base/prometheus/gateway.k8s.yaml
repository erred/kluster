apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: prometheus
spec:
  selector:
    app.kubernetes.io/name: ingressgateway
  servers:
    - port:
        name: http-prometheus
        number: 80
        protocol: HTTP
      hosts:
        - prometheus.k.seankhliao.com
      tls:
        httpsRedirect: true
    - port:
        name: https-prometheus
        number: 443
        protocol: HTTPS
      hosts:
        - prometheus.k.seankhliao.com
      tls:
        mode: MUTUAL
        credentialName: k-mtls
        minProtocolVersion: TLSV1_3
