apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: httpbin
spec:
  components:
    - name: httpbin
      type: frontend
      properties:
        image: docker.io/kennethreitz/httpbin:latest
        port: 80
