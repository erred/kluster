apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: httpbin
spec:
  selector:
    app.kubernetes.io/name: ingressgateway
  servers:
    - port:
        name: http-httpbin
        number: 80
        protocol: HTTP
      hosts:
        - httpbin.k.seankhliao.com
      tls:
        httpsRedirect: true
    - port:
        name: https-httpbin
        number: 443
        protocol: HTTPS
      hosts:
        - httpbin.k.seankhliao.com
      tls:
        mode: MUTUAL
        credentialName: k-mtls
        minProtocolVersion: TLSV1_3
