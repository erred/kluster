apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeMode: Filesystem
  volumeName: grafana
