apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitolite
spec:
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  volumeMode: Filesystem
  volumeName: gitolite
