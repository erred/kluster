apiVersion: v1
kind: Pod
metadata:
  name: alpine
spec:
  containers:
    - name: alpine
      image: alpine
      command:
        - sleep
        - "2073600"
  #     volumeMounts:
  #       - name: x
  #         mountPath: /data
  # volumes:
  #   - name: x
  #     persistentVolumeClaim:
  #       claimName: cockroachdb-data-cockroachdb-0
