apiVersion: apps/v1
kind: Deployment
metadata:
  name: alpine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alpine
  template:
    metadata:
      labels:
        app: alpine
      name: alpine
    spec:
      containers:
        - name: alpine
          image: alpine
          command:
            - sleep
            - "2073600"
