apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: etcd
  name: etcd
spec:
  selector:
    app.kubernetes.io/name: etcd
  type: ClusterIP
  ports:
    - port: 2379
      targetPort: 2379
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: etcd
  name: etcd-headless
spec:
  selector:
    app.kubernetes.io/name: etcd
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 2379
      targetPort: 2379
