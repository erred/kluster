apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: tls-prod
  labels:
    use-cloudflare-solver: "true"
spec:
  secretName: tls-prod
  dnsNames:
    - api.seankhliao.com
  issuerRef:
    name: acme-prod-issuer
    kind: ClusterIssuer
