apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/part-of: observability
resources:
  # local resources
  - namespace.k8s.yaml
  # local dir
  - grafana
  - jaeger
  - node-exporter
  - otel-collector
  - prometheus
