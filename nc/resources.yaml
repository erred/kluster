apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nc
spec:
  template:
    spec:
      containers:
        - name: web
          resources:
            requests:
              cpu: 10m
              memory: 20Mi
        - name: websecure
          resources:
            requests:
              cpu: 10m
              memory: 20Mi
