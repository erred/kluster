namespace: monitor
commonLabels:
  app: prometheus
resources:
- deployment.yaml
- rbac.yaml
- service.yaml
- volume.yaml
configMapGenerator:
- files:
  - prometheus.yml
  name: prometheus
images:
- name: prom/prometheus
  newTag: v2.17.1
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
